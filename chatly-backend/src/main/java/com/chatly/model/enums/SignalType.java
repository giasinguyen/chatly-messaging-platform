package com.chatly.model.enums;

public enum SignalType {
    OFFER,
    ANSWER,
    ICE_CANDIDATE,
    INITIATE,
    END,
    GROUP_INITIATE,
    GROUP_JOIN,
    GROUP_OFFER,
    GROUP_ANSWER,
    GROUP_LEAVE
}
